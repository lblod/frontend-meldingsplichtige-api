<div class="au-o-grid au-o-grid--flush au-u-margin-bottom-tiny">
  <div class="au-o-grid__item au-u-1-2">
    <AuLabel {{did-insert this.loadData}}>{{@field.label}}</AuLabel>
  </div>
  <div class="au-o-grid__item au-u-1-2 au-u-text-right">
    {{#if this.isRequiredField}}
      <AuPill>Verplicht</AuPill>
    {{/if}}
  </div>
</div>
{{#each this.files as |file|}}
  <VoMuFileCard @file={{file}} @onDelete={{this.removeFile}}/>
{{/each}}
<VoMuFileDropzone @onFinishUpload={{this.addFile}} @maxFileSizeMB="1024"/>
{{#if this.errors}}
  <WuAlert isError={{true}}>
    {{#each this.errors as |error|}}
      <li><AuHelpText @error={{"error"}}>{{error.resultMessage}}</AuHelpText></li>
    {{/each}}
  </WuAlert>
{{/if}}
